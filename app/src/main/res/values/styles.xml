<resources>

    <!-- Base application theme. -->
    <!-- <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar"> -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="alertDialogStyle">@color/colorPrimaryLight</item>

        <item name="android:fontFamily">@font/amiko</item>

        <item name="android:titleTextColor">@color/accent</item>
        <item name="editTextColor">@color/accent</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>

    </style>

    <!-- date picker theme -->
    <style name="DatePicker1" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorAccent">@android:color/black</item>
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

    <style name="DatePicker2" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorAccent">@android:color/black</item>
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

    <!-- time picker theme -->
    <style name="TimePicker1" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorAccent">@android:color/black</item>
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

    <style name="TimePicker2" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorAccent">@android:color/black</item>
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

</resources>
